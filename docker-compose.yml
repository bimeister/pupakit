version: '3.4'

services:
  pupakit:
    image: '${IMAGE_TAG}'
    build:
      context: ./
      dockerfile: Dockerfile
    environment:
      - PKG_VERSION
      - PKG_TAG
      - PUBLISH
      - NPM_REPOSITORY_LOGIN
      - NPM_REPOSITORY_PASSWORD
      - NPM_REPOSITORY_EMAIL
      - NPM_REPOSITORY_URL
